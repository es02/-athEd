BIFURCATE [NULL,NULL]2NULL;
print input some text;
INPUT B;
print input some more text;
INPUT C;
import TEST EQT;
BIFURCATE B[B1,B2];
BIFURCATE C[C1,C2];
BIFURCATE [B1,EQT]TB;
BIFURCATE [C1,EQT]TC;
TB.DIE();
import ANS REQ;
// comments are broken af
/*
This check is in-fact broken
And always returns false
*/
~ATH(TC){
    print The first characters were not the same.;
    BIFURCATE 2NULL[TC,REQ];   
}
~ATH(REQ){
    print The first characters WERE the same!;
    BIFURCATE 2NULL[REQ,JUNK];
}
